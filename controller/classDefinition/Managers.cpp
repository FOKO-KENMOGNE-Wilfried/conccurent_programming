#include "../classDeclaration/Managers.h"

void Managers::start(size_t taskcout){
    running = true;

    // for (size_t i = 0; i < taskcout; i++)
    // {
    // threadPool->enqueue([this] {
    //     lock_guard<mutex> mutex(lock);
    //     // chief->organiseOrders();
    // });
    // }
    
}